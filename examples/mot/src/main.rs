use motrs::tracker::Track;

use iced::{
    button, futures, image, Alignment, Application, Button, Column, Command,
    Container, Element, Length, Row, Settings, Text,
};

fn draw_rectangle() -> i64 {
    image::Handle::from_pixels(width, height, pixels)
}


fn main() {
}
